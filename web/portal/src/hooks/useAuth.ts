'use client';

/**
 * useAuth Hook
 * Convenient hook for accessing auth context
 * Re-exports from AuthContext for cleaner imports
 */

export { useAuth, type PlatformUser } from '@/contexts/AuthContext';
